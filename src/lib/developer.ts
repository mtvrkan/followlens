/**
 * Attribution shown in the popup footer and the settings page. Kept in one place
 * so the name and the URL can never drift apart between the two surfaces, and so
 * the link target is a single literal rather than something assembled at runtime.
 */
export const DEVELOPER_NAME = 'mtvrkan'
export const DEVELOPER_URL = 'https://mtvrkan.com'
